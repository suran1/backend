var chai = require('chai');
var chaiHTTP = require('chai-http');
var expect = chai.expect;

chai.use(chaiHTTP);

describe('My weather wunderground app', function(){
  describe('Server is running', function (){
    it('')
    chai.request('http://localhost:3000')
  }

});
